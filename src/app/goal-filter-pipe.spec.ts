import { GoalFilterPipe } from './goal-filter-pipe';

describe('GoalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GoalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
